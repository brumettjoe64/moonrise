<div class="details">
  <h2>Admin Details</h2>

  <table>
    <tr>
      <th>Description</th>
      <th>Date</th>
      <th>Time</th>
      <th>Groups</th>
      <th>Guests</th>
      <th colspan=2></th>
    </tr>

  <% @details.each do |detail| %>
    <tr>
      <td><%= detail.description %></td>
      <td><%= detail.when.strftime("%a %m/%d/%y") %></td>
      <td><%= detail.when.strftime("%l:%M%P") %></td>
      <td><%= detail.groups.map{|x| x.name}.join(", ") %></td>
      <td><%= detail.guests.map{|x| x.display_name}.join(", ") %></td>
      <td><%= link_to 'Edit', edit_detail_path(detail), class: "admin" %></td>
      <td><%= link_to 'X', detail, class: "admin", method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </table>

  </br>
  <%= link_to 'New Detail', new_detail_path, class: "button"%>
</div>